# 键盘树形选择器需求计划

## 目标

把原本基于数字输入的层级选择流程，改成一个可用键盘操作的树形选择器，适合多级目录、课程树、配置树等场景。

## 交互要求

1. 启动后先读取最上层一级目录。
2. 使用上下方向键在当前层级移动光标。
3. 在某个条目上按空格键时，该条目切换为选中状态，并用颜色区分。
4. 在上级目录条目上按回车时，进入并展开下级目录。
5. 允许逐层展开，直到最末级条目。
6. 在列表末尾固定放置一个“选好了”选项。
7. 当用户移动到“选好了”并按回车后，结束树形选择，进入下一步的正确率输入环节。

## 状态设计

1. 当前光标位置。
2. 当前展开层级。
3. 已选中条目集合。
4. 当前层级是否可展开。
5. 是否已进入确认阶段。

## 视觉反馈

1. 当前光标所在条目需要有明显高亮。
2. 已选中条目需要与未选中条目有明确颜色差异。
3. 可展开目录和末级条目需要有不同标识。
4. “选好了”选项需要始终显示在当前层级末尾。

## 流程计划

1. 读取顶层目录数据。
2. 渲染当前层级列表。
3. 监听键盘输入。
4. 上下键移动光标。
5. 空格键切换选中状态。
6. 回车键：
   - 如果当前项可展开，则进入下级目录。
   - 如果当前项是“选好了”，则提交选择结果。
7. 提交后进入下一步输入环节。

## 约束

1. 不破坏原有目录数据结构。
2. 不依赖鼠标操作。
3. 不让选中状态和展开状态互相干扰。
4. 需要支持返回上一级目录的能力，避免只能单向深入。
5. 在列表较长时，保持滚动和光标定位稳定。

## 验收标准

1. 能用键盘完成全流程选择。
2. 能明确区分“当前光标”和“已选中”两种状态。
3. 能逐级展开到最末层。
4. 能通过“选好了”结束选择流程。
5. 进入下一步时能拿到完整的已选结果。

